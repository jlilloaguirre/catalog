apiVersion: v1
kind: Secret
metadata:
  name: postgres-secret
type: Opaque
stringData:
  DB_PASSWORD: "iamasecret"
  API_KEY: "supersecretkey"

  credentials.ini: |
    [database]
    username=john
    password=iamasecret