name: My First GitHub Action

on: [push]

jobs:
  example:
    runs-on: ubuntu-latest

    steps:
      - name: Step 1
        run: |
          echo "Hello, world!"

      - name: Step 2
        run: |
          echo "Goodbye, world!"